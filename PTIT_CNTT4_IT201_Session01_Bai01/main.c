#include <stdio.h>
int main() {
    for (int i = 0; i < n; i++) {
        printf("Hello\n");
    }
    return 0;
}
// vòng lặp for chạy từ i = 0 đến i < n là chạy tổng cộng n lần
// printf("hello\n") thực hiện mỗi lần vòng lặp chạy
// Từ đó mỗi lần vòng lặp chạy là n lần
// suy ra độ phức tạp thời gian là 0(n)